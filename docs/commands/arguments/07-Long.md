# @LongNumber

The `@LongNumber` Annotation is used to define a parameter as a `long` value.

```java
@Execute
public boolean example(@LongNumber long l) {
    return true;
}
```

## Valid Parameter Types

- `Long`
- `long`