# @FloatNumber

The `@FloatNumber` Annotation is used to define a parameter as a `Float` or `float` type.

```java
@Execute
public boolean example(@FloatNumber float f) {
    return true;
}
```

## Valid Parameter Types

- `Float`
- `float`