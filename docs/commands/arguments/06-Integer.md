# @IntNumber

The `@IntNumber` annotation is used to specify that a parameter should be an integer number.

```java
@Execute
public boolean example(@IntNumber int i) {
    return true;
}
```

## Valid Parameter Types

- `Integer`
- `int`