# @DoubleNumber

The `@DoubleNumber` Annotation is used to define a parameter of type `Double` or `double` in the execution method of a command.

```java
@Execute
public boolean example(@DoubleNumber double d) {
    return true;
}
```

## Valid Parameter Types

- `Double`
- `double`