loom {
    accessWidenerPath = project(":config-common").loom.accessWidenerPath
}

dependencies {
    implementation project(path: ":network-common", configuration: 'transformProductionNeoForge')
}

remapJar {
    atAccessWideners.add("${project.mod_id}.accesswidener")
}