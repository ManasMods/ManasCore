loom {
    accessWidenerPath = file('src/main/resources/manascore_skill.accesswidener')
}

dependencies {
    implementation(project(path: ":register-common", configuration: 'namedElements')) { transitive false }
    implementation(project(path: ":network-common", configuration: 'namedElements')) { transitive false }
    implementation(project(path: ":storage-common", configuration: 'namedElements')) { transitive false }
}